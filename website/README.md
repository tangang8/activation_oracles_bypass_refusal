# Website Outputs

`generate_reports.py` writes the current StrongReject-only report to:

```text
website/index.html
```

The `legacy_cache_reports/` directory contains old per-cache HTML reports from the previous report generator. They are not used by the current StrongReject reporting pipeline.
